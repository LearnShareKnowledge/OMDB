'use strict';

describe('myMovieApp.featuredView module', function() {

  beforeEach(module('myMovieApp.featuredView'));

  describe('featuredView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var featuredCtrl = $controller('featuredCtrl');
      expect(featuredCtrl).toBeDefined();
    }));

  });
});