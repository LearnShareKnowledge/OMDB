'use strict';

describe('myMovieApp.searchView module', function() {

  beforeEach(module('myMovieApp.searchView'));

  describe('searchView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var searchCtrl = $controller('searchCtrl');
      expect(searchCtrl).toBeDefined();
    }));

  });
});